# Public site URL used for canonical metadata.
# Override this in Vercel once the production domain is final.
NEXT_PUBLIC_SITE_URL=https://terafab.vercel.app

# Optional. Used only in the browser for the interactive map section.
# Never commit a real private token or server-side secret.
NEXT_PUBLIC_MAPBOX_TOKEN=
